@using Domain.Enitity;
@model List<User>;

@{
    ViewData["Title"] = "Users";
    Layout = null;
}
@foreach(var user in Model)
{
    <p>@user.Login</p>
    <p @user.Role</p>
    <p>@user.Password</p>
}